<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myrepository">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyRepository"
        tools:targetApi="n">
        <activity
            android:name=".Activity.ApiActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.RoomNoteActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.FireStorLoginActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.RoomUserActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.FireStoreNoteActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>